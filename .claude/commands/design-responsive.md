---
description: Find and fix responsive layout, overflow, overlap, and hierarchy failures
argument-hint: "[page or flow and target viewports]"
disable-model-invocation: true
---

Invoke the `anti-slop-design` skill and fix responsive behavior for:

$ARGUMENTS

Inspect representative mobile, tablet, desktop, and wide-desktop views. Fix overflow, clipping, overlap, hidden actions, weak touch targets, unreadable line lengths, layout shifts, and desktop layouts merely stacked on mobile. Preserve the visual direction, make the smallest effective edits, and verify rendered results and automated checks.