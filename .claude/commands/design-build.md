---
description: Implement an approved visual direction as a complete, responsive interface
argument-hint: "[target and chosen direction]"
disable-model-invocation: true
---

Invoke the `anti-slop-design` skill and implement:

$ARGUMENTS

Inspect nearby components, tokens, assets, and conventions first. Build complete responsive and interaction states, reuse the existing design system, avoid generic AI-default composition and decoration, and validate rendered mobile and desktop results. Make the edits and run focused checks; do not stop at a proposal.