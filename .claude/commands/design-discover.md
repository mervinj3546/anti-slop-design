---
description: Generate three distinct, non-generic creative directions before building a UI
argument-hint: "[product, audience, page, constraints, and optional references]"
disable-model-invocation: true
---

Invoke the `anti-slop-design` skill and apply its Discover stage to:

$ARGUMENTS

Inspect existing product and brand context. Treat references as optional inspiration. If none are supplied, use genuine entropy from the bundled seed script. Produce exactly three materially different directions, each with a governing idea, visual rules, signature interaction, and main risk. Recommend one. Do not edit files; ask the user to choose or combine a direction.