---
description: Simplify an existing interface by removing decorative excess and generic AI patterns
argument-hint: "[page or component]"
disable-model-invocation: true
---

Invoke the `anti-slop-design` skill and apply its subtraction pass to:

$ARGUMENTS

Inspect the rendered UI and owning code. Remove decoration, unnecessary containers, nested cards, redundant labels, and meaningless variation unless functionally justified. Preserve content, accessibility, identity, and behavior. Make the edits, compare mobile and desktop results, and run focused checks.